<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Coffee</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f9f9f9;
      margin: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }
    h1 {
      text-align: center;
      margin-bottom: 30px;
      color: #333;
    }
    form {
      width: 350px;
      padding: 30px;
      background-color: #fff;
      border-radius: 8px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
    }
    input[type="text"] {
      width: calc(100% - 24px);
      padding: 10px;
      margin-bottom: 20px;
      border: 1px solid #ccc;
      border-radius: 4px;
      box-sizing: border-box;
      font-size: 16px;
    }
    button[type="submit"] {
      width: calc(100% - 24px);
      padding: 12px;
      border: none;
      background-color: #007bff;
      color: #fff;
      border-radius: 4px;
      cursor: pointer;
      font-size: 16px;
      transition: background-color 0.3s ease;
    }
    button[type="submit"]:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <h1>Edit Coffee</h1>
  <form action="/coffee/edit/<%= coffee._id %> method="POST">
    <input type="text" name="beanType" placeholder="Bean Type" value="<%= coffee.beanType %>" required><br>
    <input type="text" name="roastLevel" placeholder="Roast Level" value="<%= coffee.roastLevel %>" required><br>
    <input type="text" name="roastDate" placeholder="Roast Date" value="<%= coffee.roastDate %>" required><br>
    <input type="text" name="pricePerKg" placeholder="Price Per Kg" value="<%= coffee.pricePerKg %>" required><br>
    <input type="text" name="brewGuide" placeholder="Brew Guide" value="<%= coffee.brewGuide %>" required><br>
    <button type="submit">Update Coffee</button>
  </form>
</body>
</html>
